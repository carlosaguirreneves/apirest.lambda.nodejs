{
  "name": "app.code",
  "version": "1.0.0",
  "description": "Projeto de API usando API Gateway e Lambda Function",
  "main": "index.js",
  "scripts": {
    "build": "sh build",
    "deploy": "sh deploy",
    "install:g": "npm install -g serverless",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-domain-manager": "^4.1.1"
  },
  "dependencies": {
    "axios": "^0.19.2"
  }
}
